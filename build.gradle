/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.5.1/userguide/java_library_plugin.html
 */
plugins {
    id 'java-library'
    id "org.sonarqube" version "3.0"
    id 'com.github.hierynomus.license' version '0.15.0'
    id 'jacoco'
}

sonarqube {
  properties {
    property "sonar.projectKey", "markusremplbauer_CI-Test"
    property "sonar.organization", "markusremplbauer"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}

apply plugin: "jacoco"

test.finalizedBy jacocoTestReport

repositories {
    jcenter()
}

dependencies {
    api 'org.apache.commons:commons-math3:3.6.1'
    implementation 'com.google.guava:guava:29.0-jre'
    testImplementation 'junit:junit:4.13'
}

license {
    header = rootProject.file('docs/licenses/HEADER.txt')
    strictCheck = true
    ignoreFailures = true
    ext.year = Calendar.getInstance().get(Calendar.YEAR)
    ext.name = 'Dynatrace LLC'
    include "**/*.java"
}

jacocoTestReport {
    reports {
        xml.enabled true
    }
}